import React from 'react'
import { BasePage } from '../components/BasePage'

export const EcrsSystemFilter = () => {
  const pageInfo = {
    title: '篩選'
  }
  return (
    <BasePage  {...pageInfo}>
      <div>EcrsSystemFilter</div>
    </BasePage>
  )
}
