import React from 'react'
import { BasePage } from '../components/BasePage'

export const Permission = () => {
  const pageInfo = {
    title: '資料查詢'
  }
  return (
    <BasePage  {...pageInfo}>
      <div>Permission</div>
    </BasePage>
  )
}
